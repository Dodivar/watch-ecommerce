/**
 * Tokens visuels par défaut du socle — alignés sur l'échelle Tailwind v3.
 * Surchargeables par site via `theme.radius` dans `sites/<SITE_ID>/site.config.js`.
 */
export const DEFAULT_RADIUS = {
  sm: '0.125rem',
  DEFAULT: '0.25rem',
  md: '0.375rem',
  lg: '0.5rem',
  xl: '0.75rem',
  '2xl': '1rem',
  '3xl': '1.5rem',
  full: '9999px',
}
